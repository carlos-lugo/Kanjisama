# Node.js
node_modules/
backend/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Optional: If you prefer only package.json and install fresh

# Serverless Framework artifacts
.serverless/
backend/.serverless/
.sls_deployments/

# Logs
logs/
*.log

# OS generated files
.DS_Store
Thumbs.db
._*

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables file (if you ever add one)
.env
.env.*
!.env.example # Often good to commit an example env file

# AWS SAM specific files (if you were using SAM)
.aws-sam/

# Amplify CLI generated files / Sensitive info
# Make sure to check Amplify docs if you customize extensively
# Ignore sensitive local config and logs
amplify/.config/local-*
amplify/logs/
amplify/mock-data/
# Ignore cloud backend state pulled locally
amplify/#current-cloud-backend/
# Ignore specific generated backend files if not needed in Git
# Often amplify/backend/api/<api-name>/build/ is ignored
# Review based on your specific Amplify setup, but keep amplify/backend/ files defining resources.
amplify/backend/amplify-meta.json
amplify/backend/awscloudformation/

# Build output directories (if you add a build step later)
dist/
build/
public/ # If frontend build outputs here

# Zip files / Archives (unless explicitly needed)
*.zip
*.tar.gz